<div class="container-fluid">
  <div id="main" class="container-fluid">
	<div id="subNav">
    	<h3>マージソート（MergeSort）</h3>
        <p>数列を昇順（or 降順）に整列させるアルゴリズム<br><br>
        </p>
	</div>
    <a>マージソート</a>　　<a>演習問題</a>　    　<a href="/">ホーム</a>
</div><!-- /main -->
<hr>

<div id="editor" class="avoid-clicks" style="width:100%; height: 480px">
<b>//データ</b>
 配列 = [5,9,8,3,1,6,4];

<b>//目的</b>
 <span>配列を昇順に整列させる</span>

<b>//処理</b>

配列を一要素になるまでバラバラにする



マージソート(配列, left, right){
  if left+1 &lt; right
  　　中央 = (left + right)/2;
     マージソート(配列, left, 中央)
     マージソート(配列, 中央, right)
     マージ(配列, left, 中央, right)





マージ(配列, left, 中央, right)
  n1 = 中央 - left;
  n2 = right - 中央;
  左配列　L 右配列 R を用意する　L[0...n1], R[0...n2]
  for i = 0 to n1-1
    do L[i] = A[left + i]
  for i = 0 to n2-1
    do R[i] = A[mid + i]
  L[n1] = SENTINEL
  R[n2] = SENTINEL
  i = 0;
  j = 0;
  for k = left to right-1
    if L[i] &lt;= R[j]
      then A[k] = L[i]
           i = i + 1
      else A[k] = R[j]
           j = j + 1




配列を細分化
A,Bをマージする




A,Bをマージとは：
A，Bの最小値を比較

while 未確定要素がある
      未確定要素でヒープを作る
      最大値(root)と末端を入れ替える。
      入れ替えた最大値を確定済みとする
終了
</div><!-- /editor -->
<div id="anime">
    <p>画面クリックで説明</p>
    <anime>
    </anime>
</div><!-- /anime -->
<div id="sub">

</div><!-- /sub -->
</div><!-- /container -->
<hr>
<script src="/js/src-min/ace.js"></script>
<!--<script src="https://cdnjs.cloudflare.com/ajax/libs/ace/1.2.0/ext-language_tools.js"></script>-->
<script>
  var editor = ace.edit("editor");
  editor.renderer.setShowGutter(false);
  editor.setOptions({highlightActiveLine:false});
  editor.setTheme("ace/theme/monokai");//clouds,monokai,chrome
  editor.getSession().setMode("ace/mode/javascript");
    document.getElementById('editor').style.visibility="visible";
</script>
<script src="/js/d3.min.js" type="text/javascript" ></script>
<script src="/js/mergesort.js" type="text/javascript" ></script>